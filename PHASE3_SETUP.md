# Phase 3: 리포트 선택 화면 구현 가이드

## 📋 Phase 3 목표

- ✅ 리포트 선택 화면 레이아웃 (메인 대시보드)
- ✅ 고객사 검색 (자동완성)
- ✅ 즐겨찾기 등록/삭제/목록
- ✅ 연도/월 선택 (전월 기본값)
- ✅ 화면보기 모드 선택 (PC용/모바일용)
- ⏳ 웹화면 보기 / PDF 다운로드 (Phase 4/7에서 구현)

---

## 🎯 구현된 기능

### 1. **리포트 선택 화면 (`/report`)**

#### 주요 기능:
- **고객사 검색**: 자동완성 기능 (300ms debounce)
- **즐겨찾기**: 고객사 즐겨찾기 등록/삭제/목록 표시
- **날짜 선택**: 최근 12개월 선택 가능, 전월 기본값
- **화면 모드**: PC용(PDF 기준) / 모바일용(반응형)
- **액션 버튼**: 웹화면 보기, PDF 다운로드 (Phase 4/7에서 연결)

#### 컴포넌트 구조:
```
/app/report/
├── layout.tsx                    # 리포트 레이아웃 (네비게이션 포함)
├── page.tsx                      # 메인 페이지
└── components/
    ├── ReportNav.tsx             # 상단 네비게이션 (사용자 정보, 로그아웃)
    ├── CompanySearch.tsx         # 고객사 검색 (자동완성)
    ├── FavoritesList.tsx         # 즐겨찾기 목록
    ├── DateSelector.tsx          # 연도/월 선택
    ├── ViewModeSelector.tsx      # 화면 모드 선택
    └── AddToFavorites.tsx        # 즐겨찾기 추가/삭제 버튼
```

---

## 🗄️ 데이터베이스

### Favorites 테이블 (이미 생성됨)

`004_favorites_table.sql`에서 생성된 테이블:

```sql
CREATE TABLE public.favorites (
  favorite_id BIGSERIAL PRIMARY KEY,
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  cmny_id INTEGER NOT NULL REFERENCES public.companies(cmny_id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE(user_id, cmny_id)
);
```

**RLS 정책**:
- 사용자는 자신의 즐겨찾기만 조회/추가/삭제 가능
- Admin은 모든 즐겨찾기 조회 가능

---

## 🧪 테스트 시나리오

### 1. **고객사 검색 테스트**

#### ✅ 시나리오 1: 자동완성 검색
1. `/report` 접속
2. "고객사 검색" 입력창에 고객사명 입력 (예: "테스트")
3. 300ms 후 자동완성 드롭다운 표시 확인
4. 고객사 선택
5. 선택된 고객사 정보 하단에 표시 확인

#### ✅ 시나리오 2: 검색 결과 없음
1. 존재하지 않는 고객사명 입력
2. "검색 결과가 없습니다." 메시지 확인

#### ✅ 시나리오 3: 선택 해제
1. 고객사 선택 후 X 버튼 클릭
2. 선택 해제 확인

---

### 2. **즐겨찾기 테스트**

#### ✅ 시나리오 1: 즐겨찾기 추가
1. 고객사 검색 후 선택
2. (향후) 즐겨찾기 버튼 클릭
3. 즐겨찾기 목록에 추가 확인

#### ✅ 시나리오 2: 즐겨찾기에서 선택
1. 즐겨찾기 카드 클릭
2. 해당 고객사가 선택됨 확인
3. 하단에 선택된 정보 표시 확인

#### ✅ 시나리오 3: 즐겨찾기 삭제
1. 즐겨찾기 카드의 X 버튼 클릭
2. 즐겨찾기 목록에서 제거 확인

---

### 3. **날짜 선택 테스트**

#### ✅ 시나리오 1: 전월 기본값
1. `/report` 접속
2. 날짜 선택 드롭다운 확인
3. 전월이 기본값으로 선택되어 있는지 확인
   - 예: 오늘이 2025-12-24 → 기본값 2025-11

#### ✅ 시나리오 2: 다른 월 선택
1. 날짜 선택 드롭다운 클릭
2. 최근 12개월 목록 확인
3. 다른 월 선택
4. 하단에 선택된 날짜 표시 확인

---

### 4. **화면 모드 선택 테스트**

#### ✅ 시나리오 1: PC용 모드 (기본값)
1. `/report` 접속
2. "PC용 (PDF 기준)" 버튼이 선택되어 있는지 확인
3. 설명 텍스트: "PDF 출력 레이아웃과 동일한 화면으로 표시됩니다."

#### ✅ 시나리오 2: 모바일용 모드
1. "모바일용 (반응형)" 버튼 클릭
2. 버튼 스타일 변경 확인
3. 설명 텍스트: "모바일 환경에 최적화된 반응형 레이아웃으로 표시됩니다."

---

### 5. **액션 버튼 테스트**

#### ✅ 시나리오 1: 버튼 비활성화
1. 고객사 또는 날짜 미선택 상태
2. "웹화면 보기", "PDF 다운로드" 버튼 비활성화 확인

#### ✅ 시나리오 2: 버튼 활성화
1. 고객사 + 날짜 선택
2. 버튼 활성화 확인
3. "웹화면 보기" 클릭 → alert 메시지 확인 (Phase 4에서 실제 구현)
4. "PDF 다운로드" 클릭 → alert 메시지 확인 (Phase 7에서 실제 구현)

---

### 6. **네비게이션 테스트**

#### ✅ 시나리오 1: 사용자 정보 표시
1. `/report` 접속
2. 상단 우측에 사용자명, 부서명 표시 확인

#### ✅ 시나리오 2: Admin 링크
1. Admin 계정으로 로그인
2. 상단 우측에 "관리자" 버튼 표시 확인
3. 클릭 시 `/admin/users`로 이동 확인

#### ✅ 시나리오 3: 로그아웃
1. "로그아웃" 버튼 클릭
2. 로그인 페이지로 이동 확인

---

### 7. **권한 테스트**

#### ✅ 시나리오 1: 미승인 사용자
1. 미승인 사용자로 로그인
2. `/report` 접속 시도
3. `/waiting-approval`로 리다이렉트 확인

#### ✅ 시나리오 2: 승인된 사용자
1. 승인된 사용자로 로그인
2. `/report` 정상 접속 확인

---

## 🎨 UI/UX 특징

### 1. **반응형 디자인**
- Tailwind CSS 유틸리티 클래스 사용
- 모바일, 태블릿, 데스크톱 대응

### 2. **사용자 피드백**
- 로딩 상태 표시 (즐겨찾기 조회 시)
- 선택된 항목 하이라이트
- 버튼 비활성화 상태 명확히 표시

### 3. **접근성**
- 시맨틱 HTML 사용
- 키보드 네비게이션 지원
- aria-label, aria-hidden 적용

---

## 🔜 다음 단계 (Phase 4)

Phase 3 완료 후 Phase 4에서 구현할 내용:

1. **monthly_summary 테이블 생성** + 더미 데이터 3개월치
2. **리포트 요약 페이지 UI** (4x2 카드 레이아웃)
3. **수식 적용** (가동률, 전월비, 증감률)
4. **서비스 소개 섹션**
5. **"웹화면 보기" 버튼 연결** → `/report/view?cmny_id=...&year_month=...&view=pc`

---

## 📝 참고사항

### 고객사 검색 최적화
- **Debounce**: 300ms (사용자 입력 후 300ms 대기)
- **ILIKE 검색**: 대소문자 구분 없이 부분 일치
- **결과 제한**: 최대 10개

### 즐겨찾기 UX
- **중복 방지**: `UNIQUE(user_id, cmny_id)` 제약조건
- **삭제 확인**: 별도 확인 없이 즉시 삭제 (UX 간소화)
- **실시간 업데이트**: 추가/삭제 시 목록 즉시 갱신

### 날짜 선택
- **기본값**: 로그인 시점 기준 전월
- **범위**: 최근 12개월
- **형식**: `YYYY-MM` (예: `2025-11`)

---

## ✅ Phase 3 완료 체크리스트

- [x] 리포트 레이아웃 생성 (`layout.tsx`)
- [x] 상단 네비게이션 구현 (`ReportNav.tsx`)
- [x] 고객사 검색 (자동완성) 구현 (`CompanySearch.tsx`)
- [x] 즐겨찾기 목록 표시 (`FavoritesList.tsx`)
- [x] 즐겨찾기 추가/삭제 버튼 (`AddToFavorites.tsx`)
- [x] 날짜 선택 (전월 기본값) (`DateSelector.tsx`)
- [x] 화면 모드 선택 (`ViewModeSelector.tsx`)
- [x] 선택된 정보 표시 (하단 요약)
- [x] 액션 버튼 (웹화면 보기, PDF 다운로드)
- [x] 권한 체크 (미승인 사용자 차단)
- [ ] 전체 테스트 완료

---

## 🐛 알려진 이슈

없음

---

## 💡 개선 아이디어 (향후)

1. **고객사 검색 개선**
   - 최근 검색 기록 표시
   - 검색 결과에 고객사 로고 표시

2. **즐겨찾기 개선**
   - 드래그 앤 드롭으로 순서 변경
   - 즐겨찾기 그룹 기능 (부서별, 프로젝트별)

3. **날짜 선택 개선**
   - 캘린더 UI로 변경
   - 날짜 범위 선택 (시작월~종료월)

---

**Phase 3 구현 완료!** 🎉

다음 명령어로 테스트를 시작하세요:

```bash
npm run dev
```

그리고 브라우저에서 `http://localhost:3000/report`로 접속하세요.

